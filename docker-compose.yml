services:
  ts-mcp-tools:
    image: node:26-alpine
    container_name: ts-mcp-tools
    restart: always
    working_dir: /app
    volumes:
      - .:/app:ro
    command: ["node", "src/index.ts"]
    networks:
      - mcp_network

networks:
  mcp_network:
    external: true
    name: mcp-tools-net
